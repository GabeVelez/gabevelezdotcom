extras/
projects_WIP/
.DS_Store

# Security - prevent credential files
.credentials/
*.json
!package.json
!package-lock.json
!tsconfig.json
!halfabuck/**/*.json
*.key
*.pem
.env
.env.*
*-oauth.keys.json
service-account*.json
gcp-*.json

# Data files - prevent CSV and other data files from MCP work
*.csv
*.xlsx
*.xls

# Firebase debug logs - generated by Firebase CLI MCP processes
firebase-debug.log
firebase-debug.*.log

# Design files - InDesign and Photoshop source files. Never served, and
# titlescreen.psd alone is 30MB that every clone and every deploy carried.
*.indd
*.psd
